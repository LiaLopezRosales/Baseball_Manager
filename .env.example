# Copia este archivo a `.env` y rellena los valores reales.
# NUNCA commitees tu `.env` real (contiene credenciales).

# Django
SECRET_KEY=tu_secret_key_aleatoria_aqui
DEBUG=True
ALLOWED_HOSTS=localhost,127.0.0.1

# PostgreSQL
DB_NAME=campeonatos
DB_USER=tu_usuario_postgres
DB_PASSWORD=tu_password_postgres
DB_HOST=localhost
DB_PORT=5432
